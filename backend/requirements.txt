# ── Web framework ───────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.18

# ── PDF parsing ─────────────────────────────────────
pypdf==5.1.0

# ── LangChain core + pluggable providers ────────────
# Keep the whole stack on the langchain-core 0.3.x line — the <0.4 cap is load-
# bearing: without it, pip upgrades core to 1.x for the provider packages and
# breaks langchain/community/ollama (which require core <0.4).
langchain==0.3.14
langchain-core>=0.3.86,<0.4
langchain-community==0.3.14
langchain-ollama==0.2.3          # local models
langchain-google-genai>=2.0,<3   # Google Gemini (cloud)
langchain-anthropic>=0.3,<0.4    # Anthropic Claude (cloud)
# langchain-openai also powers LLM_PROVIDER=azure_openai and, via a custom
# base_url, LLM_PROVIDER=openai_compatible (OpenRouter / Together / DeepSeek /
# Fireworks / vLLM / LM Studio / llama.cpp). The <0.4 cap keeps core on 0.3.x —
# langchain-openai 1.x requires langchain-core 1.x.
langchain-openai>=0.3,<0.4       # OpenAI + Azure OpenAI + OpenAI-compatible

# Heavier, cloud-specific SDKs (Vertex AI, Bedrock, Groq, Mistral, Cohere) are
# opt-in — see requirements-providers.txt — so the default image stays small.

# ── Embeddings (local, no API key) ──────────────────
fastembed>=0.4,<0.8

# ── Database ────────────────────────────────────────
neo4j==5.27.0

# ── Graph algorithms ────────────────────────────────
# Louvain community detection runs in Python: the Neo4j image is
# neo4j:5-community, which ships APOC but NOT the GDS plugin, so gds.* is
# unavailable. networkx keeps the algorithms portable and testable.
networkx>=3.2,<4

# ── Config ──────────────────────────────────────────
python-dotenv==1.0.1
pydantic-settings==2.7.0
