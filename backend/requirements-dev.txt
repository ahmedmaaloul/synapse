# Dev / test dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
ruff==0.16.0

# Research experiments (backend/research/) — bootstrap CIs, graph topology.
# Not needed for the app or its test suite; only to reproduce the studies.
numpy>=1.26,<3
